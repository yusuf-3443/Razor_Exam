@page
@model UpdateTransactionModel
@{
    ViewData["Title"] = "UpdateTransaction";
}

<!DOCTYPE html>
<html>
<head>
    <meta charset="utf-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <title>Update New Account</title>
    <link href="https://cdnjs.cloudflare.com/ajax/libs/simple-line-icons/2.4.1/css/simple-line-icons.min.css" rel="stylesheet">
    <style>
        body {
            background-color: #dee9ff;
        }

        .registration-form {
            padding: 50px 0;
        }

        .registration-form form {
            background-color: #fff;
            max-width: 600px;
            margin: auto;
            padding: 50px 70px;
            border-top-left-radius: 30px;
            border-top-right-radius: 30px;
            box-shadow: 0px 2px 10px rgba(0, 0, 0, 0.075);
        }

        .registration-form h1 {
            text-align: center;
            margin-bottom: 30px;
        }

        .registration-form .item {
            border-radius: 20px;
            margin-bottom: 25px;
            padding: 10px 20px;
        }

        .create-account {
            border-radius: 30px;
            padding: 10px 20px;
            font-size: 18px;
            font-weight: bold;
            background-color: #5791ff;
            border: none;
            color: white;
            margin-top: 20px;
            display: block;
            text-align: center;
            text-decoration: none;
        }

        .create-account:hover {
            border: 4px solid #0e5ef1;
        }

        .create-cancel {
            border-radius: 30px;
            padding: 10px 20px;
            font-size: 18px;
            font-weight: bold;
            background-color: rgb(4, 212, 4);
            border: none;
            color: white;
            margin-top: 20px;
            display: block;
            text-align: center;
            text-decoration: none;
        }

        .create-cancel:hover {
            border: 4px solid rgb(1, 124, 1);
            color: white;
        }
    </style>
</head>
<body>
    <div class="registration-form">
        <form method="post">
            <h1>@ViewData["Title"]</h1>

            <div class="form-group">
                <input asp-for="Transaction.FromAccountId" type="text" class="form-control item" id="username" placeholder="FromAccountId">
                <span asp-validation-for="Transaction.FromAccountId" class="text-danger"></span>
                <input asp-for="Transaction.ToAccountId" type="text" class="form-control item" id="username" placeholder="ToAccountId">
                <span asp-validation-for="Transaction.ToAccountId" class="text-danger"></span>
                <input asp-for="Transaction.Amount" type="text" class="form-control item" id="username" placeholder="Amount">
                <span asp-validation-for="Transaction.Amount" class="text-danger"></span>
                <input asp-for="Transaction.TransactionDate" type="text" class="form-control item" id="username" placeholder="TransactionDate">
                <span asp-validation-for="Transaction.TransactionDate" class="text-danger"></span>
            </div>
            <div class="form-group">
                <button type="submit" class="btn btn-block create-account">Update Transaction</button>
                <a asp-page="/Transaction/GetTransactions" class="btn btn-block create-cancel">Cancel</a>
            </div>
        </form>
    </div>
</body>
</html>
